# Work Log 2026-06-21

## 琴行管理系统 v2.4 移动端 + v2.2 PC 端 响应式重构

### 完成的工作

#### 前端页面重构 (7 个页面 + 2 个布局组件)
1. **Dashboard.jsx** - 完全重写
   - 移动端:蓝色渐变头部 + 4 个毛玻璃统计卡(学生总数/本月收入/待上课/教师数) 对齐设计稿
   - 桌面端:4 个数据卡(学生/教师/剩余总课时/本月净利润) + 双栏(课时不足/快捷操作 3x2 网格) + 课时统计
   - 响应式:分别渲染移动/桌面端,避免组件在两种布局间变形

2. **MobileMine.jsx** - 简化为设计稿 2 项菜单(账号设置/系统设置) + 整行红色退出按钮
   - 紫色渐变头像(居中 + 用户名 + 角色)
   - 20px 圆角白色卡片,菜单项带 40px 圆角图标块

3. **Students.jsx** - 移动端
   - 顶部蓝色渐变头 + 搜索框
   - 新增 filter-tabs 横滚(全部/钢琴/小提琴/吉他/声乐)
   - 卡片右侧"课时"大数字(橙/红/绿三色提醒)
   - 标签气泡用 11px 字号 + 2px 圆角设计稿风格

4. **Courses.jsx** - 移动端对齐设计稿(签到/充值/记录 3-Tab 顺序)
   - 课程卡片 20px 圆角对齐设计稿
   - 签到卡片:ClipboardCheck 图标 + 标题/副标题
   - 签到按钮绿色,充值按钮蓝色
   - 增加"课时不足"黄色警示条

5. **Transactions.jsx** - 移动端紫色渐变财务汇总
   - 蓝紫渐变 finance-summary(账户余额 + 本月收入/支出 毛玻璃卡)
   - 4-Tab 横排(全部/收入/支出/工资)底部 3px 蓝色激活条
   - 工资 Tab:黄色提示条 "工资由系统根据实际上课记录自动结算"
   - 工资卡片红色 Briefcase 图标 + 负数红色金额

6. **Layout.jsx** - 响应式分支
   - 移动端:TopBar(汉堡菜单) + 抽屉式侧边栏 + BottomNav
   - 桌面端:固定侧边栏 + 主内容
   - window resize 监听自动切换

7. **Sidebar.jsx** - 适配 Layout
   - 接受 onClose 和 onLogout props
   - 移动端汉堡打开抽屉,导航时自动关闭

8. **BottomNav.jsx** - 5-Tab 底部导航保持不变
9. **App.css** - 新增 fade-in / slide-in 动画 + mobile 安全区

### 后端关键修复

**问题**: 旧 mysql2 在 node22 缺失 promise 子模块,启动报 Cannot find module 'mysql2/promise'
**修复**:
- `server/models/db.js` 重写为 better-sqlite3 (sqlite 本地数据库)
- 数据库文件: `/Users/yishunxin/WorkBuddy/20260329231952/music_school.db`
- 默认账号 admin / 123456 自动创建
- API 全部接口保持兼容

**Vite proxy**:
- `client/vite.config.js` 添加 /api -> localhost:3001 代理(开发环境)
- 生产环境 Caddy 已配置类似代理

### 构建验证
- `npm run build` ✓ 1642 modules, 345ms
- 所有页面(移动+桌面)实测通过 agent-browser 截图验证
  - /login /dashboard /teachers /course-types /students /courses /transactions
  - 3 个 Tab(签到/充值/记录)切换正常
  - 4 个 Tab(全部/收入/支出/工资)切换正常

### 已知细节
- 教师/学生/课程类型初始数据库为空(全新 sqlite),界面显示"暂无数据"Empty 状态
- 工资 Tab 数据为硬编码示例,真实数据需 teacher_salary 表产生记录
- 开发环境用 sqlite,生产环境(云服务器)仍用 MySQL
- 用户偏好: 喜欢直接执行,不要冗余确认

### 服务器/工具
- dev 后端: 3001 端口 (后台运行)
- dev 前端: 5173 端口 (后台运行)
- 数据库: /Users/yishunxin/WorkBuddy/20260329231952/music_school.db
