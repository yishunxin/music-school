# 2026-05-01 日志

## UI设计稿修改 (v2.3 → v2.4)

### 手机版图标优化
1. Dashboard快捷操作：签到(`plus`→`check-circle`)、充值(`credit-card`→`wallet`)、工资管理(`dollar-sign`→`banknote`)
2. 课时不足提醒：图标从 `radio` 改为 `alert-triangle`
3. 教师管理：头像和底栏图标从 `music` 改为 `graduation-cap`
4. 课程类型：底栏图标从 `layout` 改为 `music`
5. 财务页：工资(`dollar-sign`→`banknote`)、收入(`dollar-sign`→`wallet`)、支出(`dollar-sign`→`receipt`)
6. 课时管理签到：图标从 `music` 改为 `check-circle`

### 手机版修改 (v2.2 → v2.3)
1. 财务模块去掉"生成月结"按钮，改为提示语
2. 课时管理去掉"退课"Tab（保留签到/充值/记录）
3. 首页"今日待上课"改为"课时不足提醒"
4. 课程类型页面去掉"启用/停用"标签
5. "我的"页面只保留：账号设置、系统设置、退出登录

### 电脑版修改
1. Dashboard快捷操作中"教师管理"图标从 `teacher` 改为 `graduation-cap`
2. 课时管理去掉"退课"Tab（保留充值/签到/记录）
